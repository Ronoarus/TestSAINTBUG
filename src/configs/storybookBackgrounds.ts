const values = [
  {
    name: 'white',
    value: '#fff',
  },
  {
    name: 'black',
    value: '#191919',
  },
];

const defaultWhite = {
  default: 'white',
  values,
};
const defaultBlack = {
  default: 'black',
  values,
};

export { defaultWhite, defaultBlack };
